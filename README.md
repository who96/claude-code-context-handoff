# claude-code-context-handoff
 Automatic context preservation for Claude Code sessions. Uses PreCompact and SessionStart hooks to extract and restore key context before/after auto-compaction, preventing intelligence    degradation in long sessions. Zero manual intervention required.
